# snippets

Small pieces of code worth keeping, but not worth a repo each.

- [`kotlin/script`](kotlin/script) — run an external command, get its exit code and output in a lambda.
- [`kotlin/databricks-file-sync`](kotlin/databricks-file-sync) — Ktor client for the Databricks Files REST API, plus a one-shot folder-to-volume upload job.
